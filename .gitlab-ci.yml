stages:
  - build

image: docker:latest

variables:
  DOCKER_DRIVER: overlay2
  NAMESPACE: orangetalent/gitlab-ci

services:
  - docker:dind

before_script:
  - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"

Ubuntu 7.3:
  stage: build
  variables:
    IMAGE_VERSION: "7.3-ubuntu"
  script:
    - docker pull php:$IMAGE_VERSION || true
    - docker pull $NAMESPACE:$IMAGE_VERSION || true
    - docker build --compress --cache-from $NAMESPACE:$IMAGE_VERSION --build-arg VCS_REF=$CI_COMMIT_SHORT_SHA --build-arg BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') -t $NAMESPACE:$IMAGE_VERSION -f php/7.3/ubuntu/Dockerfile .
    - docker push $NAMESPACE:$IMAGE_VERSION
  when: manual

Ubuntu 7.4:
  stage: build
  variables:
    IMAGE_VERSION: "7.4-ubuntu"
  script:
    - docker pull php:$IMAGE_VERSION || true
    - docker pull $NAMESPACE:$IMAGE_VERSION || true
    - docker build --compress --cache-from $NAMESPACE:$IMAGE_VERSION --build-arg VCS_REF=$CI_COMMIT_SHORT_SHA --build-arg BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') -t $NAMESPACE:$IMAGE_VERSION -f php/7.4/ubuntu/Dockerfile .
    - docker push $NAMESPACE:$IMAGE_VERSION
  when: manual

Ubuntu 7.4 xdebug:
  stage: build
  variables:
    IMAGE_VERSION: "7.4-ubuntu-xdebug"
  script:
    - docker pull php:$IMAGE_VERSION || true
    - docker pull $NAMESPACE:$IMAGE_VERSION || true
    - docker build --compress --cache-from $NAMESPACE:$IMAGE_VERSION --build-arg VCS_REF=$CI_COMMIT_SHORT_SHA --build-arg BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') -t $NAMESPACE:$IMAGE_VERSION -f php/7.4/ubuntu-xdebug/Dockerfile .
    - docker push $NAMESPACE:$IMAGE_VERSION
  when: manual

Glyphhanger:
  stage: build
  variables:
    IMAGE_VERSION: "glyphhanger"
  script:
    - docker pull orangetalent:$IMAGE_VERSION || true
    - docker build --compress --cache-from orangetalent:$IMAGE_VERSION --build-arg VCS_REF=$CI_COMMIT_SHORT_SHA --build-arg BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') -t orangetalent:$IMAGE_VERSION -f glyphhanger/Dockerfile .
    - docker push orangetalent:$IMAGE_VERSION
  when: manual
